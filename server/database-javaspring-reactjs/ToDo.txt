// Todo:
// Allow "getprofile" API to accept userID or username.
// Fetch the Profile-in-View UserProfile by username.
// Put Photo in Profile Header

Profile Feed should be reading Posts from the User posts<> list, not from the Posts database. 
In a real database that would mean you would be scanning millions of messages

// BUG:
// Navigate from user2 to user1, and the Page won't reset & refresh correctly.